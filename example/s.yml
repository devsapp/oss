edition: 1.0.0
services:
  oss-demo:
    component: ../lib
    access: default
    props:
      region: cn-hangzhou
      bucket: hfs-oss-bucket
      codeUri: './build'
      acl: 
      static:
        index: index.html
        error: index.html
        childIndex:
      cors:
      refer: